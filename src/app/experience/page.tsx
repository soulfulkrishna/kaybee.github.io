import { Shell } from "@/components/Shell";import { experience } from "@/lib/content";export const metadata={title:"Work.Log"};export default function ExperiencePage(){return <Shell title="WORK.LOG"><h1>Experience</h1><div className="timeline">{experience.map(item=><article key={`${item.org}-${item.period}`}><div className="timeline-marker"/><div><h2>{item.role}</h2><p><strong>{item.org}</strong> · {item.period}</p><p>{item.note}</p></div></article>)}</div></Shell>}
