import { Shell } from "@/components/Shell";import { education,experience,highlights,profile } from "@/lib/content";export const metadata={title:"Resume"};export default function ResumePage(){return <Shell title="RESUME.DOC"><div className="resume"><h1>{profile.name}</h1><p>{profile.headline}</p><h2>Experience</h2>{experience.map(item=><p key={`${item.org}-${item.period}`}><strong>{item.role} — {item.org}</strong><br/>{item.period}<br/>{item.note}</p>)}<h2>Education</h2>{education.map(item=><p key={`${item.degree}-${item.school}`}><strong>{item.degree}</strong><br/>{item.school} · {item.period}</p>)}<h2>Selected highlights</h2><ul>{highlights.map(item=><li key={item}>{item}</li>)}</ul></div></Shell>}
